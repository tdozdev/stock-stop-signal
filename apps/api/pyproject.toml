[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sss-api"
version = "0.1.0"
description = "SSS FastAPI skeleton"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
]

[tool.setuptools]
packages = ["sss_api"]
